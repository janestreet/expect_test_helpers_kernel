(library
 ((name        expect_test_helpers_kernel)
  (public_name expect_test_helpers_kernel)
  (libraries (
    core_kernel
    re.pcre
    sexp_pretty
    ))
  (js_of_ocaml())
  ))

(enforce_style ())

(jbuild_version jane_street)
